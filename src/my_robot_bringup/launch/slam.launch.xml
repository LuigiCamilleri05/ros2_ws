<launch>
    <!-- Arguments -->
    <arg name="use_sim_time" default="true" />
    
    <!-- Robot URDF path -->
    <let name="urdf_path" 
        value="$(find-pkg-share my_robot_description)/urdf/my_robot.urdf.xacro" />
    
    <!-- Config paths -->
    <let name="slam_config" 
        value="$(find-pkg-share my_robot_bringup)/config/slam_toolbox.yaml" />
    <let name="gazebo_bridge_config" 
        value="$(find-pkg-share my_robot_bringup)/config/gazebo_bridge.yaml" />
    <let name="rviz_config" 
        value="$(find-pkg-share my_robot_bringup)/config/slam_rviz.rviz" />

    <!-- Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- Launch Gazebo with world -->
    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="$(find-pkg-share my_robot_bringup)/worlds/test_world.sdf -r" />
    </include>

    <!-- Spawn robot in Gazebo at origin -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description -x 0 -y 0 -z 0.1" />

    <!-- Gazebo-ROS bridge -->
    <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(var gazebo_bridge_config)" />
    </node>

    <!-- Odom to TF broadcaster (required for SLAM) -->
    <node pkg="my_robot_bringup" exec="odom_to_tf.py" name="odom_to_tf">
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- SLAM Toolbox - using sync mode -->
    <node pkg="slam_toolbox" exec="sync_slam_toolbox_node" name="slam_toolbox" output="screen">
        <param from="$(var slam_config)" />
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- Auto-activate SLAM lifecycle node -->
    <node pkg="my_robot_bringup" exec="slam_activator.py" name="slam_activator" output="screen">
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

    <!-- RViz for visualization -->
    <node pkg="rviz2" exec="rviz2" output="screen" args="-d $(var rviz_config)">
        <param name="use_sim_time" value="$(var use_sim_time)" />
    </node>

</launch>
